# CPP CLI BOILERPLATE

How to use:

```shell
git clone https://github.com/Raisess/cpp-cli-boilerplate cli-name
cd cli-name
bash setup.sh
make
make install
cli-name --hello
// print: Hello, world!
```

that is all.
